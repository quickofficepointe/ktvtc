<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('messages', function (Blueprint $table) {
            $table->id();
            $table->string('name');
            $table->string('email');
            $table->string('phone')->nullable();
            $table->text('message');

            // Track which admin viewed it first
            $table->foreignId('first_seen_by')->nullable()->constrained('users')->nullOnDelete();

            // Action taken: e.g. pending, replied, resolved, archived
            $table->enum('status', ['pending', 'viewed', 'replied', 'resolved', 'archived'])->default('pending');

            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('messages');
    }
};
